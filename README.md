# Maximum Sum Subset of Array

```python
def max_subarry(arr):
    pass
```
- My version of the max subarray algorithm, written in Python.
- Computes the maximum contiguous sub array sum in one traversal
